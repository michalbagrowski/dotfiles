alias fuck='sudo $(history -p \!\!)'
alias kurwa='sudo $(history -p \!\!)'
if [ "$(uname)" == "Darwin" ]; then
   alias git=hub
fi
alias psql='docker run -it --rm  postgres psql'
alias dockergc='docker run --rm --userns host -v /var/run/docker.sock:/var/run/docker.sock -v /etc:/etc spotify/docker-gc'
